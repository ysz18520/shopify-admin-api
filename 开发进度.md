# Shopify 后端开发进度记录

---

## 2026-06-04 域名迁移 + 投票系统清理

### 变更内容
- 域名从 `coollaa.com` 迁移到 `sifanonline.com`
- 移除投票系统（designs/contacts）全部代码
- 移除 Store 中的 `isVotingEnabled` 字段
- 管理后台删除设计管理页面和相关逻辑
- 数据库同步清理（删除 designs、design_votes、contacts 表）
- Caddy 配置更新为新域名

### 新访问地址
| 服务 | 地址 |
|------|------|
| 管理后台 | https://admin.sifanonline.com |
| API | https://api.sifanonline.com/api |

---

## 2026-05-28 服务器迁移

### 变更内容
- 从旧服务器 `107.172.230.133` 迁移到新服务器 `212.50.228.178`
- 翻译插件移除
- Docker Compose + Caddy 反向代理部署

---

## 2026-05-21 多店铺管理后台架构改造

### 核心变更
- 支持多店铺数据隔离（超管看全部，店铺管理员只看自己）
- 重写登录鉴权（JWT payload 扩展 role + site）
- 前端管理后台全面适配多店铺权限
- 生产环境部署更新（Docker 镜像已重建）

### 后端变更

#### 1. JWT 鉴权扩展 (`src/middleware/auth.ts`)
- Payload 结构: `{ username, role: 'super' | 'site', site: string }`
- 新增 `siteFilterMiddleware`: 店铺角色强制注入 `req.query.site`，超管可自由筛选

#### 2. 多账号登录 (`src/features/admin/service.ts`)
- 移除 bcrypt 硬编码密码方案
- `admin/admin` → role='super', site='all'
- `{site}/{site}` → role='site', site=店铺名

#### 3. Admin API 数据隔离 (`src/features/admin/controller.ts` + `routes/admin.ts`)
- 所有 admin 路由增加 `siteFilterMiddleware`
- 店铺管理接口：增删改查 + 重命名

#### 4. 多店铺配置 (`src/config/booking.ts`)
- `COOLLAA_BOOKING` → `SITE_CONFIGS` 映射表
- `getSiteConfig(site)` 动态获取配置
- `getAllSites()` 返回所有店铺名

#### 5. Prisma Schema
- Store 模型（店铺信息 + 功能开关）
- User 模型（管理后台用户）

### 前端变更 (管理后台)

#### 1. Auth Store (`src/stores/auth.ts`)
- 新增 `role`, `site`, `viewSite` ref
- 新增 `isSuper` computed
- 新增 `setViewSite()` 方法（超管切换查看店铺）

#### 2. 页面适配

| 页面 | 变更内容 |
|------|----------|
| Layout.vue | 动态标题，右上角角色标签 |
| Dashboard.vue | 超管显示店铺筛选器 |
| BookingList.vue | 店铺列 + 店铺筛选器 |
| AvailabilityConfig.vue | 超管需先选店铺 |
| StoreList.vue | 店铺管理页面（超管） |
| StoreProfile.vue | 店铺信息页面 |
| PreviewTest.vue | 店铺筛选 |
| ApiInfo.vue | 角色信息展示 |

---

## 2026-05-15 首次部署

- 基础预约系统上线
- 单店铺（coollaa）
- Docker Compose 部署
