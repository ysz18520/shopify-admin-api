# 文件上传接口对接文档

> 更新时间：2026-06-08
> 适用店铺：apopresent（以及其他开启了上传功能的店铺）

---

## 一、上传文件

### 请求

| 项目 | 内容 |
|------|------|
| 方法 | `POST` |
| 地址 | `https://api.sifanonline.com/api/coollaa/upload?site={店铺标识}` |
| Content-Type | `multipart/form-data` |

**Query 参数**

| 参数 | 必填 | 说明 |
|------|------|------|
| `site` | 是 | 店铺标识，如 `apopresent` |

**Body 参数（form-data）**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `file` | File | 是 | 要上传的文件 |

### 响应 - 成功（200）

```json
{
  "id": "cmq4kwc5a0000p0010eorv2fj",
  "site": "apopresent",
  "fileName": "product-photo.jpg",
  "fileKey": "shopify/apopresent/b2121111130a41dd8b5f20e9a7b45c62.jpg",
  "fileUrl": "https://image.sifanonline.com/shopify/apopresent/b2121111130a41dd8b5f20e9a7b45c62.jpg",
  "fileSize": 102400,
  "mimeType": "image/jpeg",
  "uploadedBy": null,
  "createdAt": "2026-06-08T02:15:16.606Z"
}
```

**字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 文件记录 ID |
| `fileName` | string | 原始文件名 |
| `fileUrl` | string | **文件访问地址（给用户展示用这个）** |
| `fileSize` | number | 文件大小（字节） |
| `mimeType` | string | MIME 类型 |
| `createdAt` | string | 上传时间（ISO 8601） |

### 响应 - 失败

| 状态码 | 错误信息 | 说明 |
|--------|----------|------|
| `400` | `site is required` | 未传 site 参数 |
| `400` | `No file uploaded` | 未选择文件 |
| `400` | `文件大小超过限制（最大 50MB）` | 文件超出限制 |
| `400` | `不支持的文件类型 .exe，允许的类型：.jpg,.png` | 文件类型不在允许列表 |
| `404` | `Store not found` | 店铺不存在 |
| `500` | `Internal server error` | 服务器错误 |

---

## 二、前端对接示例

### Shopify Liquid 中使用

```html
<form id="uploadForm">
  <input type="file" id="fileInput" name="file" />
  <button type="submit">上传文件</button>
</form>

<div id="result"></div>

<script>
document.getElementById('uploadForm').addEventListener('submit', async function(e) {
  e.preventDefault();

  const fileInput = document.getElementById('fileInput');
  if (!fileInput.files[0]) {
    alert('请选择文件');
    return;
  }

  const formData = new FormData();
  formData.append('file', fileInput.files[0]);

  try {
    const response = await fetch('https://api.sifanonline.com/api/coollaa/upload?site=apopresent', {
      method: 'POST',
      body: formData
    });

    const result = await response.json();

    if (response.ok) {
      // 上传成功，result.fileUrl 就是文件访问地址
      document.getElementById('result').innerHTML =
        '<p>上传成功！</p>' +
        '<p>文件地址：<a href="' + result.fileUrl + '" target="_blank">' + result.fileUrl + '</a></p>' +
        '<p>文件大小：' + (result.fileSize / 1024).toFixed(1) + ' KB</p>';
    } else {
      // 上传失败
      alert('上传失败：' + (result.error || '未知错误'));
    }
  } catch (error) {
    alert('网络错误：' + error.message);
  }
});
</script>
```

### JavaScript fetch（通用）

```javascript
async function uploadFile(file, site = 'apopresent') {
  const formData = new FormData();
  formData.append('file', file);

  const response = await fetch(
    `https://api.sifanonline.com/api/coollaa/upload?site=${site}`,
    {
      method: 'POST',
      body: formData,
    }
  );

  const result = await response.json();

  if (!response.ok) {
    throw new Error(result.error || '上传失败');
  }

  return result;
}

// 使用示例
const input = document.querySelector('input[type="file"]');
input.addEventListener('change', async () => {
  try {
    const result = await uploadFile(input.files[0]);
    console.log('文件地址:', result.fileUrl);
  } catch (err) {
    console.error(err.message);
  }
});
```

### AJAX jQuery（老项目）

```javascript
var formData = new FormData();
formData.append('file', $('#fileInput')[0].files[0]);

$.ajax({
  url: 'https://api.sifanonline.com/api/coollaa/upload?site=apopresent',
  type: 'POST',
  data: formData,
  processData: false,
  contentType: false,
  success: function(result) {
    console.log('文件地址:', result.fileUrl);
  },
  error: function(xhr) {
    var error = xhr.responseJSON ? xhr.responseJSON.error : '上传失败';
    alert(error);
  }
});
```

---

## 三、当前店铺配置

| 配置项 | 值 |
|--------|-----|
| 默认最大文件大小 | 50MB |
| 默认允许文件类型 | `*`（全部类型） |

> 管理员可在管理后台「店铺管理 → 上传配置」中修改每个店铺的限制。

---

## 四、注意事项

1. **跨域**：后端已配置 CORS，Shopify 域名可直接调用
2. **文件存储**：文件存储在七牛云 CDN，域名 `image.sifanonline.com`，全球加速
3. **文件命名**：上传后系统自动生成唯一文件名，不会覆盖已有文件
4. **文件路径格式**：`shopify/{店铺标识}/{uuid}.{扩展名}`
5. **无需登录**：上传接口不需要 Authorization token（C 端公开接口）
6. **大小限制**：服务端硬限制 60MB，店铺配置默认 50MB
